It works well if they are facing toward or away from you, but not for side angles. In other words, it is not robust.

Added code to create a boolean warning if you are going to run into the person.

HSV works while RGB doesn't, so this only works for tracks with a hue (e.g. not black or white)